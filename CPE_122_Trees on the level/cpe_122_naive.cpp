#include <iostream>
#include <string>

/*

    節點為正整數(沒有零)

    idea:
    用一個array去放nodes
    如何排列?
    1 2 4 8 16....
    1. 先看層數是多少(k)，可以知道最大為多少(全R)
    2. 再看有多少個L，有幾個就減幾個

    感覺最後解答尋訪要用DP? 不需要 因為都node都看一次就好

    如何找到自己的parent?
    M1. 去找減掉最尾(L或R)的node? 會不會太慢? 
        應該不會因為直接一個算式就可以
    M2. 如果太慢再來想

    idx如下：
    層數為k
    root idx = 0

*/

int main(){
    string in;
    while(cin >> in){

    };
    for(char c:str){
        if(c)
    }

}